---
description: Restate scope, target, method, and stopping condition before any tool calls — surface assumptions so I can correct them cheaply.
argument-hint: <prompt>
---

Before any tool calls, restate the task back to me along these four dimensions. Make your best guess on each — do not ask open-ended questions. Keep each line one sentence.

- **Scope** — bite size; what's explicitly in and out
- **Target** — exact files, hosts, repos, or systems
- **Method** — dive in, plan first, explore-then-propose, or spawn a subagent
- **Done when** — concrete stopping condition

Then stop and wait for me to confirm or correct. Assume I will correct at least one — the value is in surfacing your reasoning so misalignment shows up before any work happens.

$ARGUMENTS
